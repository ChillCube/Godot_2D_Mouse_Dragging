# Godot_2D_Mouse_Dragging
An addon for dragging objects with the mouse in godot 2D

# Submodules
This addon is reliant on other addons. Make sure to always use
```git pull --recurse-submodules --remote```
inside this addon, to ensure all of the submodules are synchronized. 
